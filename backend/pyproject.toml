[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["app"]
omit = ["app/creative_mock.py", "app/__pycache__/*"]

[tool.coverage.report]
show_missing = true
fail_under = 70
